import React from 'react';
import PropTypes from 'prop-types';
import styled from 'styled-components';

const Jobs = ({ data }) => {
  // Container id='jobs'
  return <div>Jobs</div>;
};

export default Jobs;
